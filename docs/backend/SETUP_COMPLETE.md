# Historical backend setup milestone

> **Historical reference — not current operating guidance.** This document
> recorded a prior setup state and contained routes, worker behavior, and mock
> assumptions that no longer match the current code.

Use [the backend README](../../backend/README.md) to start the API and worker,
[API testing](API_TESTING.md) for current routes, and [mock mode](MOCK_MODE_GUIDE.md)
only for fixture-based frontend work.
