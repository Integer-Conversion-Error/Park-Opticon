# Historical Docker setup milestone

> **Historical reference — not current operating guidance.** This completion
> snapshot predates the separate `alert-worker` service and should not be used
> as a deployment or security checklist.

Use [Docker quick start](QUICKSTART_DOCKER.md) for local development and
[deployment requirements](DOCKER_DEPLOYMENT.md) for the current operational
boundaries.
