# Historical scripts and commands catalog

> **Historical reference — not current operating guidance.** This catalog
> contains obsolete worker intervals, mock/deployment commands, and paths.

Use the `package.json`/`Makefile` scripts in each active component and the
[current setup guide](../setup/SETUP_GUIDE.md).
