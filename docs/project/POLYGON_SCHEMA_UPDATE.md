# Historical polygon schema update

> **Historical reference.** Runtime migration 22 and fresh bootstrap migration
> 010 normalize `parking_spots.geofence` to `geometry(POLYGON, 4326)`.

Use [DATABASE_SCHEMA.md](DATABASE_SCHEMA.md) and
[the migration guide](../../backend/migrations/README.md) for the current path.
